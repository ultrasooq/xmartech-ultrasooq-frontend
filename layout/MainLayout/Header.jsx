import React  from 'react';
import { withRouter } from 'next/router';


const Header = (props) => {
   return (
      <header>

         Header
      </header>
   );
}

export default withRouter(Header);